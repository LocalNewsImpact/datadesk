# Copy to .env and adjust. `make setup` does this when .env is missing.
# Never commit .env.

DJANGO_SECRET_KEY=dev-only-not-a-real-secret
DJANGO_DEBUG=1
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# Google sign-in. Leave blank locally: with no client id the provider is
# unconfigured, the landing page says so, and superusers use the admin
# login — no OAuth credentials needed to run the project.
GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_SECRET=
# Comma-separated hosted domains allowed to sign in; empty = unrestricted
# (development only).
ALLOWED_AUTH_DOMAINS=localnewsimpact.org

# Crawler database, read-only (SCOPE.md §1). Unset locally: the crawler
# alias falls back to an empty sqlite file and the landing page reports
# "not connected". To read production through the Cloud SQL Auth Proxy:
#   cloud-sql-proxy --port 5440 mizzou-news-crawler:us-central1:mizzou-db-prod
#CRAWLER_DB_USER=datadesk_ro
#CRAWLER_DB_PASSWORD=
#CRAWLER_DB_HOST=127.0.0.1
#CRAWLER_DB_PORT=5440
