# Runtime dependencies. Development tooling lives in requirements-dev.txt.
Django>=5.2,<6.0
django-allauth[socialaccount]>=65.0
psycopg[binary]>=3.2
google-cloud-bigquery>=3.25
google-cloud-storage>=2.18
# Subscriber credentials for paywalled publishers. They live in the
# crawler's project and the console stores only the secret's name;
# see review/credentials.py.
google-cloud-secret-manager>=2.20
ftfy>=6.2
gunicorn>=23.0
whitenoise>=6.7
# So collectstatic writes .br beside .gz. Without it WhiteNoise can only
# offer gzip, and every embed pays about a quarter more bytes for the
# same files -- 250KB of county outlines instead of 190KB.
brotli>=1.1

# The Source Directory as an installed app (ROADMAP.md item 14). Pinned
# to a tag rather than a branch: a moving dependency makes a build
# unreproducible, and the tag is what `version` in that repository's
# pyproject declares. Public, so no credential is involved.

# The shared review-note contract (lnic-contracts). review/dispositions.py
# imports it at module scope, so the queue view cannot render without it.
#
# It was in requirements-dev.txt only, which is what a local checkout and
# CI install -- so every test passed, and the deployed image, which
# installs this file, did not have it. /review/queue/ 500'd on
# ModuleNotFoundError while a reproduction against the same databases
# returned 200.
#
# The release tarball rather than git+https, for the same reason the
# crawler uses it: no build stage needs git to install a tarball, and one
# of these two images does not have git.
lnic-contracts @ https://github.com/LocalNewsImpact/lnic-contracts/archive/refs/tags/v0.3.0.tar.gz
