# Development and CI tooling. Toolchain matches the MizzouNewsCrawler
# pre-push gate: ruff, black, isort, mypy, then tests.
-r requirements.txt
pytest>=8.0
pytest-django>=4.8
ruff>=0.6
black>=24.0
isort>=5.13
mypy>=1.11
# Reading .github/workflows in tests: the three triggers are a contract.
PyYAML>=6.0

# Reading outside directories into data/sources. A development-time tool:
# the readings are committed, so nothing at runtime parses HTML.
beautifulsoup4>=4.12

