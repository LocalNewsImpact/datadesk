# Development and CI tooling. Toolchain matches the MizzouNewsCrawler
# pre-push gate: ruff, black, isort, mypy, then tests.
-r requirements.txt
pytest>=8.0
pytest-django>=4.8
ruff>=0.6
black>=24.0
isort>=5.13
mypy>=1.11
# Reading .github/workflows in tests: the three triggers are a contract.
PyYAML>=6.0

# Reading outside directories into data/sources. A development-time tool:
# the readings are committed, so nothing at runtime parses HTML.
beautifulsoup4>=4.12

# The shared contracts. Pinned to a tag, never a branch: the shape a
# service writes and another reads must not change under a test run.
lnic-contracts @ git+https://github.com/LocalNewsImpact/lnic-contracts@v0.1.0
