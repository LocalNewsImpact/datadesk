"""Accounts and access (SCOPE.md §2.1).

Google-only sign-in via django-allauth with a hosted-domain restriction,
and the three-role model (viewer, editor, admin) implemented as Django
groups. No local-password signup path exists.
"""
